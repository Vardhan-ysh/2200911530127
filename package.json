{
  "name": "url-shortener-backend",
  "version": "1.0.0",
  "description": "HTTP URL Shortener Microservice with reusable logging middleware",
  "private": true,
  "scripts": {
    "start": "ts-node backend/src/app.ts",
    "dev": "ts-node backend/src/app.ts",
    "build": "tsc -b logging-middleware backend/tsconfig.json",
    "test": "echo no-tests"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.12.7",
    "@types/node-fetch": "^2.6.13",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}

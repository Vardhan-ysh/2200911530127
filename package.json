{
  "name": "url-shortener-backend",
  "version": "1.0.0",
  "description": "HTTP URL Shortener Microservice with reusable logging middleware",
  "private": true,
  "scripts": {
    "start": "node dist/app.js",
    "dev": "ts-node src/app.ts",
    "build": "prisma generate && tsc -b",
    "test": "echo no-tests"
  },
  "dependencies": {
    "@prisma/client": "^6.15.0",
    "dotenv": "^17.2.2",
    "express": "^4.19.2",
    "prisma": "^6.15.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.12.7",
    "@types/node-fetch": "^2.6.13",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
